# Open Questions & Investigations

## Active

| Q | Question | Why | Status | Gap | Opened | Investigating | Closed |
|---|----------|-----|--------|-----|--------|---------------|--------|
| Q-013 | What do the 4 SND: preamble bytes (A6 F8 02 80) encode, and what do the 3 init bytes (10 C6 46) mean? | SND container header and DAT init field may contain version/format flags needed for playback | ✅ | G19 | E-003 | E-007 | **DAT init bytes [0x10, 0xC6, 0x46]:** NOT referenced by ADL.DRV — confirmed unused padding/metadata. Writing them to OPL2 kills audio output. Safe to ignore. SND preamble bytes are standard Sierra chunked container (SND:+size+data). | E-007 |
| Q-014 | How are OPL2 registers encoded in FM patch payloads (after the 12-byte header)? | Required to programme YM3812 for sound effects | ✅ | G19 | E-005 | E-005 | SCI28 format confirmed by ScummVM source. Upper bits in masked bytes are ignored by driver. |
| Q-014a | What is the format of sequence data after the 28-byte SCI instrument in FM patches? | Required to play FM sound effects with correct note timing | ✅ | G19 | E-005 | E-012 | **Channel state parameter block.** TIM2.EXE copies the sequence bytes into a per-channel sound structure (segment 0x43C6, 30-byte entries). The structure stores pitch bend, volume, pan, modulation, patch#, note velocity. ADL.DRV is called via `CALL FAR CS:[0x01ED]` with BP=function index (0=VersionInfo, 1=LoadData, 2=Init, 4=NoteOff, 5=NoteOn, 7=Controller, 8=ProgramChange, 9=PitchWheel). The uniform byte distribution is explained by raw numerical channel parameters (not MIDI events). ANTGRV sequence is only 10 bytes (5 shorts) — too small for event sequence. Sequence is NOT parsed by ADL.DRV. ADL.DRV call sites confirmed at file offsets 0x41B09, 0x41AB8, 0x41BEA, 0x41BDE. | E-012 |
| Q-014b | Are the KSL=0 (all modulators) and feedback=0 (all patches) patterns correct, or signs of format misalignment? | Affects accuracy of instrument decode | ✅ | G19 | E-005 | E-005 | Confirmed correct: KSL=0 (byte[0]&0x03 always 0) and FB=0 (byte[2]&0x07 always 0) per ScummVM SCI28 decode |
| Q-015 | What sample rate are the RAW files? | Required to play digital audio at correct speed | ✅ | G20 | E-003 | — | R-003 |
| Q-016 | What is the tick-to-seconds conversion factor? | Required to simulate physics at correct speed | ✅ | G11 | E-009 | E-009 | 60 Hz (VBL-synchronized). Evidence: VGA port 0x3DA VBL wait in rendering (0x37031), main loop iteration per VBL (0x53242-0x53375), tick counter at [0x6873]. One tick = 1/60 sec ≈ 16.67 ms. |
| Q-017 | What collision detection algorithm does TIM2 use? | Required for accurate physics simulation | ✅ | G12 | E-010 | E-010 | **AABB collision detection** confirmed. Physics loop at 0x54EAB iterates per-part; per-part step at 0x55353 calls gravity integration (0x5361E) then collision detection (0x54459). AABB overlap test compares four extents per part (computed by 0x5428B). Collision resolution (0x57250) divides overlap by 2 and adjusts positions. Connection following for belt/rope constraints via 0x98/0x9A fields. |
| Q-018 | How many iterations per tick for rope/belt/chain constraints? | Required for accurate constraint physics | ✅ | G13 | E-011 | E-011 | **Single-pass constraint solver** (1 iteration per tick). Three frame passes handle ropes/belts: pass 3 (0x550DC) initializes, pass 4 (0x5514A) calls constraint solver at segment 0x30:0x54F2, pass 5 (0x551A4) is alternative. No inner loop — typical for 1994 DOS games. Solver enforces distance constraint between connected parts. The `behavior` field for ropes encodes the segment length. Belt (type 8) and Steel Cable (type 76) follow same architecture. |
| Q-019 | What do the undefined flag bits in the 48-byte part struct mean? | Required to correctly initialise part states | ✅ | G4 | E-008 | E-012 | **All 5 unknowns resolved.** flags_1 0x0020 = physics bias toggle (EXE 0x53923). flags_1 0x0040 = constraint skip flag (EXE 0x5542F). flags_3 0x0008 = gravity_x direction. flags_3 0x0100 = read-only mechanical part marker (~4% of mechanical parts, metadata only, no runtime test pattern). flags_2 0x0040: confirmed unused. |
| Q-020 | What is the gravity constant in pixels per tick squared? | Required to simulate falling objects | ✅ | G11 | E-009 | E-009 | Gravity depends on part type: base 0x1000-0x1C00. gravity_y = base/4 ≈ 1024-1792 units. After >>9: ~2.0-3.5 pixels/tick². At 60 Hz: 7200-12600 pixels/sec². Env gravity (272) selects table row. Physics integration at EXE 0x53622. |
| Q-021 | How is the level editor data block structured? | Required to save/load editor state | ✅ | G5 | E-006 | E-012 | **RESOLVED — does not exist in the built-in levels.** The alleged "editor block" was a parsing artifact: the old fixed 48-byte part reader misaligned after belt/rope/pulley entries. With the corrected variable-size parser (knt47 layout: belt=52, rope=54, pulley=56), the gap disappears completely. No editor state block exists in the 203 built-in level files. User-saved .TIM files may have editor state, but none are in the repository. |
| Q-022 | What is the struct layout used by FUN_31f7_1265 at segment 0x32DA? | Ghidra maps all 3 entry points (32DA:0498/B8/D6) to the same function — need to understand the jump table dispatch | ✅ | M10 | E-013 | E-013 | **All 3 entry points are within FUN_31f7_1265 (283B).** 32DA:0498 = `ADD BX,BX; MOV [BX+0x78CE],AX` (sound buffer store). 32DA:04D6 = `PUSH [BX+0x3A46]; CALL 0x4000:5880` (FM playback trigger). 32DA:04B8 = similar with param from `[BX+0x3A42]`. Not a generic dispatch — it's the sound system coupled to state transitions. |
| Q-023 | What are the state limit constants (DAT_5b41_19cc etc.) in FUN_31f7_3bd2? | These define state counter bounds for the state execution engine — needed to understand state cycling behavior | ✅ | M10 | E-013 | E-015 | **Runtime-initialized globals from a loaded resource — cannot statically extract.** 7 constants (indices 3,4,5,7,8,9,0xFFFF) lazily loaded via `FUN_4551_0370` from a TB-format resource. The resource ID at `DAT_5b41_09ec` is in BSS (0x0000 at Ghidra). Init uses retry loop until external code sets the resource ID. All 6 DAT_5b41_19c* values confirmed 0x0000 in EXE. | R-015 |
| Q-025 | Does W2's init value 0x6315 at offset 0x18 overlap with ANM state IDs? | Writes a packed bitfield as initial state — need to validate against the 89 known ANM state IDs | ✅ | M10,G8 | E-013 | E-013 | **No overlap.** ANM state IDs range 1-52 (max=52). 0x6315 = 25365 >> 52. Packed bitfield of behavior flags (bits 0,2,4,8,9,13,14). Confirmed not an ANM state ID. |

## Closed

| Q | Question | Why | Answer | Closed by |
|---|----------|-----|--------|-----------|
| Q-001 | What algorithm does RESOURCE.MAP use to hash filenames? | Required to look up resources by name | Weighted-sum + XOR + ROL8 + IMUL/CWD with weight table "RichRayl@JTP\0RES" | R-001 |
| Q-002 | Where is the hash function located in TIM2.EXE? | Required to verify the algorithm | CS:0x05F4, ROL32 helper at CS:0x199B | R-001 |
| Q-003 | Why do some hashes differ by 1 in the high word? | Explains ft/ft-1 pairing pattern | CWD sign-extension: when IMUL product >= 0x8000, DX becomes 0xFFFF | R-001 |
| Q-004 | Do MAP header indices override EXE defaults? | Required to compute hashes correctly | Yes: MAP [0,1,4,7] overrides EXE [0,1,6,7] | R-001 |
| Q-005 | Do .ANM files have a Sierra container wrapper? | Required to parse animations | Yes, all 433 use ANM: wrapper with TB inner format | R-002 |
| Q-006 | How is the TB animation header structured? | Required to parse animation sections | 16 bytes: magic + version + 4 section sizes + width + height | R-002 |
| Q-007 | How many Section D opcodes exist? | Required to interpret render bytecode | Exactly 6 (0-5), no unknown opcodes in any file | R-002 |
| Q-008 | What are the DRAW_BMP/DRAW_RECT flag semantics? | Required to render sprites correctly | BMP: bit 0=hflip, bit 1=vflip. RECT: bit 15=absolute, bits 0-3=mode | R-002 |
| Q-009 | What is the overall container format of TIM2.SX? | Required to play any sound from the game | Sierra SND/INF/DAT/TAG chunked container with 4 chunk types | R-003 |
| Q-010 | How are instruments indexed in TIM2.SX? | Required to look up sounds by ID | INF chunk: 5-byte header + 38 × 6-byte entries (sound_id UINT16LE + offset UINT32LE) | R-003 |
| Q-011 | What are the DAT patch header fields? | Required to separate instrument data from metadata | 12 common bytes: sound_id(UINT16) + type(UINT8) + mode(UINT8) + unknown(UINT8) + parameter(UINT16) + zero(UINT16) + init[3] | R-003 |
| Q-012 | What encoding are the RAW sound files? | Required to play digital audio | Unsigned 8-bit PCM, no header, center=128, 113 files IDs 3001-3754, sample rate 11025 Hz | R-003/auditory |
| Q-014 | How are OPL2 registers encoded in FM patch payloads (after the 12-byte header)? | Required to programme YM3812 for sound effects | SCI28 (Sierra 13+13+2 byte) format confirmed by ScummVM `engines/sci/sound/drivers/adlib.cpp`. Low-bit extraction: byte[0]&3→KSL, byte[1]&F→MULT, byte[2]&7→FB, byte[3]&F→AR, byte[4]&F→SL, byte[5]→EGS(bool), byte[6]&F→DR, byte[7]&F→RR, byte[8]&3F→TL, byte[9]→AM(bool), byte[10]→VIB(bool), byte[11]→KSR(bool), byte[12]→ALG(inv bool). Upper bits ignored by driver. | E-005 |
Empirically confirmed via DOSBox 0.74-3 OPL2 playback: captured LOOP_TONE.COM output shows same dominant frequency (2605 Hz) as synthetic ANTGRV WAV (2616 Hz). | E-005 |
| Q-014b | Are KSL=0 and feedback=0 correct? | Accuracy of instrument decode | Yes: KSL=0 (byte[0]&3 always 0) and FB=0 (byte[2]&7 always 0) confirmed by ScummVM decode. Plausible for simple FM sound effects. | E-005 |
| Q-016 | Tick-to-seconds conversion factor | Physics timing | 60 Hz VBL-synchronized. VGA port 0x3DA VBL wait at 0x37031, main loop per VBL at 0x53242-0x53375. Tick = 1/60 sec. | E-009 |
| Q-020 | Gravity constant in pixels/tick² | Physics simulation | ~2.0-3.5 px/tick² depending on part type. At 60 Hz: 7200-12600 px/sec². | E-009 |
| Q-024 | Sound parameter table at seg 0x554C:0x3A30-0x3C00 | Maps part types/channels to FM patch IDs | **Partially decoded.** 3 sections: (1) 6-entry pair table at 0x3A42 mapping (value, part_type); (2) 16-entry offset table at 0x3A60 for pt0-15; (3) W6 index arrays at 0x3BBA/0x3BBE/0x3BC2. E-013 addresses corrected by 0x10 (Ghidra segment offset). 0x3A30=8 (not 120). Per-part offsets DO NOT use single base. | R-016 |
| Q-014a | Format of sequence data after SCI28 instrument in FM patches | Required for correct FM playback | **Channel state parameter block** — feeds per-channel sound struct at segment 0x43C6. ADL.DRV API mapped (9 functions). Not MIDI, not OPL2 reg/val pairs. | E-012 |
| Q-018 | Rope/belt/chain constraint iterations per tick | Required for accurate constraint physics | **Single-pass** (1 iter/tick). Three frame passes (0x550DC, 0x5514A, 0x551A4). Solver at 0x30:0x54F2 enforces distance constraint. | E-011 |
| Q-019 | Undefined flag bits in the 48-byte part struct | Required to correctly initialise part states | **All 5 unknowns resolved.** flags_1 0x0020 = physics bias toggle. flags_1 0x0040 = constraint skip. flags_3 0x0008 = gravity_x direction. flags_3 0x0100 = mechanical part marker (metadata only). flags_2 0x0040 = unused. | E-012 |
| Q-022 | Struct layout of FUN_31f7_1265 at segment 0x32DA | 3 entry points mapped to same function | **Sound system entry points**, not dispatch: sound_store_A (0x32DA:0498), sound_play_A (0x32DA:04B8), sound_play_B (0x32DA:04D6). All call FAR 0x4000:5880 for FM playback. | E-013 |
| Q-025 | Does W2's init value 0x6315 overlap with ANM state IDs? | Validate against state-part-matrix | **No overlap.** ANM state range 1-52. 0x6315 = 25365 >> 52. Packed bitfield (bits 0,2,4,8,9,13,14) — behavior flags, not state ID. | E-013 |

## Superseded

| Q | Question | Why | Superseded by | Reason |
|---|----------|-----|---------------|--------|
| Q-022 | What is the format of SX_3???.RAW audio data beyond byte encoding? | Sample rate, stereo/mono, bit depth confirmation | Q-015 | Q-015 resolved sample rate; superseded |