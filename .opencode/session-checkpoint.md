# opencode session checkpoint
> Auto-generated by opencode agent. Update this file at the start of each session.

## Last Updated
2026-06-05

## Session Summary
- Read GDD: `re_crd_gdd/docs/clean_room_gdd/compiled.html` (Game Design Document for TIM2 remake)
- Read MVP spec: `mvp.md` (Minimum Viable Product for Rube Goldberg chain)
- Explored `mvp_rube_goldberg/` codebase thoroughly via subagent
- Wrote gap analysis: `mvp_rube_goldberg/doc/gap-analysis.md`

## Key Findings

### GDD vs MVP Reality
- **Physics**: MVP has correct 7-pass loop but runs at variable framerate, not 60Hz fixed
- **Language**: GDD/mvp.md calls for Zig GDExtension; MVP is 100% GDScript
- **Architecture**: MVP is 960-line monolith (Main.gd); GDD calls for separation (Zig core + Godot frontend)
- **Parts**: 13 hardcoded in chain; GDD expects 149 playable + 116 scenery
- **Levels**: No YAML serialization; everything hardcoded
- **UI**: No parts bin, no controls, simulation auto-runs
- **SOLVE.RES**: Not data-driven; chain is hardcoded
- **Audio**: Stub (no-op)
- **BDD tests**: 0 tests written

### Gap Analysis Summary (from `mvp_rube_goldberg/doc/gap-analysis.md`)
| Priority | Area |
|---|---|
| P0 | No GDExtension/Zig — critical to mvp.md spec |
| P0 | No level serialization (YAML) — everything hardcoded |
| P0 | No UI at all |
| P1 | No SOLVE.RES table |
| P1 | No level editor |
| P1 | No separation of concerns |
| P2 | No audio system |
| P2 | No solution condition evaluator |
| P2 | No test framework |
| P3 | Missing Zig SoA architecture |
| P3 | No menu / user journeys |

### What's Working Well
1. ANM bytecode renderer (faithful to original format)
2. 7-pass simulation loop in correct order
3. State machine with deterministic RNG
4. Spatial grid broadphase collision
5. End-to-end Rube Goldberg chain works

## Recommended Next Steps (Priority Order)
1. **Refactor Main.gd** into separate files (PhysicsEngine, Board, Part, ChainManager)
2. **Implement YAML level format + LevelLoader.gd**
3. **Build minimal UI** (play/stop, debug parts bin, goal display)
4. **Data-drive SOLVE.RES** (replace hardcoded chain with collision lookup table)
5. **Implement 60Hz fixed-tick loop** (decouple from framerate)
6. **Add 1,000-run determinism test**
7. **Port physics core to Zig/GDExtension**

## Decisions Made
- Gap analysis written to `mvp_rube_goldberg/doc/gap-analysis.md`
- No decisions made yet on implementation order (pending user direction)

## Phase A — Data-Driven Levels ✅ DONE (commit 171cb47)
**Goal:** Replace hardcoded _setup_board() with YAML level file + loader.

**Implemented:**
- `data/levels/mvp_chain.yaml` — 13-part chain as declarative YAML (meta, physics, fixed_parts, moving_parts, solution AABB_overlap, chain_script stub)
- `Scripts/LevelLoader.gd` — hand-rolled YAML parser (122 lines, no external deps)
- `Scripts/PartFactory.gd` — Part factory from YAML data (59 lines)
- `Scripts/SolutionCondition.gd` — declarative win condition evaluator (51 lines)
- `Scripts/Main.gd` — modified: `_load_level()` + `solution.check()` replacing hardcoded coord check

**Preserved (not yet data-driven):**
- Chain logic (`_chain_logic()`) — hardcoded, uses parts[] indices
- SOLVE.RES — not yet a lookup table; chain_script block in YAML is unused stub

**Next phase:** Phase B — Minimal UI wrapper

## Phase B — Minimal UI Wrapper ✅ DONE (commit 45bb9ec)
**Goal:** Wrap simulation with actual controls so no code editing needed to interact.

**Implemented:**
- `Scripts/SimUI.gd`: CanvasLayer with Play/Stop/Reset buttons, speed slider
  (0x/1x/2x/3x/4x), goal label, debug parts bin (7 types click-to-spawn)
- `Scenes/Main.tscn`: updated with SimUI node, VBoxContainer, PartsLayer node
- `Scripts/Main.gd`:
  - No auto-start (waits for Play button in GUI mode; auto-starts headless)
  - Speed multiplier: tick_accum accumulator
  - Signal wiring: speed_changed, reset_requested, part_spawn_requested
  - _find_part_named(), _get_archetype_for_type() helpers

**Fixes applied during testing:**
1. `class_name` discovery → `preload()` (Godot 4 doesn't auto-discover classes in Scripts/)
2. YAML parser too fragile → native JSON (`JSON.parse_string`)
3. `fixed_parts` / `moving_parts` split breaks hardcoded `parts[]` indices → single `parts[]` with `is_moving` in JSON
4. `Main.Part` circular dependency → preload Main.gd in helper scripts
5. `hex_to_int()` Godot 4 API fix → decimal values in JSON instead
6. Button signal connections → wired in SimUI._ready()

**Verified:**
- Simulation runs end-to-end, WIN at tick 326
- Timer fires at tick 119, gate opens at tick 277, laser hit same tick
- Physics: ball bounces on floor, rope constraint pulls gate, fan pushes balloon

**Preserved:**
- Chain logic still hardcoded (_chain_logic uses parts[] indices)
- `chain_script` block in YAML → removed (JSON format, no chain_script)
- LevelLoader.gd (YAML parser) — dead code, kept for reference

## Phase D — 60Hz Fixed Tick + Determinism ✅ DONE (commit 96f7b46)
**Goal:** Decouple physics from render framerate; validate determinism.

**Implemented:**
- `tick_accum_ms` accumulator driven by wall-clock (Time.get_ticks_msec())
- Fixed 60Hz: `ms_per_tick = 1000.0 / 60.0` (~16.67ms per tick)
- sim_speed multiplier now applies to elapsed time (elapsed_ms * sim_speed)
- `--determinism-test` CLI arg: runs 50 simulations @ 4x speed
- `_deterministic_log_tick()`: captures ball_x/y/vel_y, balloon_x/y, laser_powered, gate_y per tick
- `_run_deterministic_test()`: compares all logs, returns bool
- Verified: 50 runs, all 327 ticks, 5 events, identical state — **PASS**

**Bug found & fixed:** Reset didn't clear `parts[]`, causing stale state counters
across Play cycles (fixed in aacce28).

**Preserved:**
- Chain logic still hardcoded (_chain_logic uses parts[] indices)
- Level format: JSON with single parts[] array

## Pending User Decisions
- Phase C: Refactor monolith into PhysicsEngine/Board/Part/ChainManager?
- Phase E: Data-drive chain logic (SOLVE.RES from JSON)?
- Phase F: Audio system (replace AudioManager stub)?
- Phase G: Level editor (tool modes)?